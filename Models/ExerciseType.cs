namespace WorkoutTracker.Models
{
    public enum ExerciseType
    {
        Weighted = 1,    // e.g., Machine Pulldown, T-bar
        Bodyweight = 2,  // e.g., Pull ups
        TimeBased = 3    // e.g., Planks (for future-proofing)
    }
}
